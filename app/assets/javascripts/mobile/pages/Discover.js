import React from 'react';
import {
  Container,
  Group,
} from 'amazeui-touch';

import Tool from '../Tool'

const Discover = React.createClass({

  render() {

    return (
      <p>find</p>
    );
  },
});

export default Discover;
