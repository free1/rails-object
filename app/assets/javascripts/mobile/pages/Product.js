import React from 'react';
import {
  Container,
  Group,
} from 'amazeui-touch';
import Tool from '../Tool'

const Product = React.createClass({
  render: function() {
    return (
      <h1>test</h1>
    );
  },
});

export default Product;
