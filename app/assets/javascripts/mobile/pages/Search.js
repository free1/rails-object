import React from 'react';
import {
  Container,
  Group,
} from 'amazeui-touch';
import Tool from '../Tool'

const Search = React.createClass({
  render: function() {
    return (
      <h1>test Search</h1>
    );
  },
});

export default Search;
